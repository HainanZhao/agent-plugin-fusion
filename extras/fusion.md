---
description: Fusion — shortcut for the /fusion:run plugin command
argument-hint: '[@agent[:model] …] <task>  e.g. @gemini:gemini-3.1-pro add retry logic'
---

Invoke the **`fusion:run`** plugin command (from the installed `fusion` plugin)
with exactly these arguments, passed through verbatim:

$ARGUMENTS

Use the Skill tool to run `fusion:run`. Do not reimplement the fusion logic
here — this file only exists so the command can be typed as `/fusion` instead of
`/fusion:run`.
